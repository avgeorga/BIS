SELECT 
    C.COUNTRY, 
    SUM(O.QUANTITY * P.UNITPRICE) AS TOT 
FROM 
    ORDERS O 
    JOIN PRODUCTS P ON O.STOCKCODE = P.STOCKCODE 
    JOIN CUSTOMERS C ON O.CUSTOMERID = C.CUSTOMERID 
GROUP BY 
    C.COUNTRY 
ORDER BY 
    TOT DESC;